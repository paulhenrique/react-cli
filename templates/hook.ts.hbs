/**
* @file use{{pascalCase name}}.tsx
* @description use{{pascalCase name}} hook.
*/
interface Use{{pascalCase name}}Props {
  // Params here
  fromProps: string;
}
export const use{{pascalCase name}} = ({ fromProps }:  Use{{pascalCase name}}Props ) => {
  // TODO:
  const exampleProperty = fromProps + 'example';

  return { 
    exampleProperty
  };
}

export default use{{pascalCase name}}
